# CI smoke: launch the Simulator Dev/Release build and assert the home title.
# Matches the Expo template home screen (app/(tabs)/index.tsx).
appId: com.marginallybetter.converter
---
- launchApp
- extendedWaitUntil:
    visible: "MB Converter"
    timeout: 30000
- assertVisible: "MB Converter"

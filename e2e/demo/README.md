# Demo flows

Maestro flows used for the **final** headed Simulator demo recording only.

CI should run `e2e/` and **exclude** this folder.

When ready (Epic 5), add something like `full-tour.yaml` and record with:

```sh
./scripts/record-demo.sh e2e/demo/full-tour.yaml /tmp/mb-demo.mp4
```

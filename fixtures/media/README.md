# Media fixtures

`tiny.mp4` is a small real H.264/AAC MP4 (~23 KB, ISO BMFF with `ftyp`) for
path/probe smoke tests and Maestro sample-file navigation.
Command-builder unit tests do not require real media bytes.
